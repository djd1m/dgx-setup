# 05. Ouroboros — рецепт установки для AI-агента

**Цель:** развернуть [razzant/ouroboros](https://github.com/razzant/ouroboros) на DGX Spark / GB10 (aarch64, DGX OS)
из исходников, headless (`ouroboros server`, `127.0.0.1:8765`), с моделями через Ollama по
OpenAI-совместимому провайдеру. Проверено на коммите `554b3ee`, версия **6.64.1**.

**Что ты ставишь:** не кодинг-ассистент. Агент **читает и переписывает собственный исходный код**,
ведёт свой git-репозиторий и крутит фоновый цикл «сознания» между задачами. Управляется
«конституцией» из 13 принципов в [`BIBLE.md`](https://github.com/razzant/ouroboros/blob/main/BIBLE.md).
Дословно из [README](https://github.com/razzant/ouroboros/blob/main/README.md):

> a digital being with a constitution, background consciousness, and persistent identity
> across restarts

**Условные обозначения в этом файле:**

- `NOT VERIFIED` — правдоподобно, но авторами не документировано и не проверялось. Не выдавай за факт.
- `НЕТ В ИСТОЧНИКЕ` — данных нет в человекочитаемой инструкции. Не выдумывай их. Спроси человека.

---

## Предусловия

| Условие | Проверка | Если не выполнено |
|---|---|---|
| Выполнена инструкция [00-ollama.md](00-ollama.md) | Ollama отвечает на `http://localhost:11434` | Останови установку, сначала 00-ollama.md |
| Python 3.10+ | `python3 --version` | Поставь Python 3.10+ |
| Git | `git --version` | Поставь git |
| Получено решение человека по `## Стоп-условия` | см. ниже | **Не устанавливай.** Спроси человека |

Node, Rust и Docker для установки из исходников **не нужны**. Docker нужен только если человек
выбрал контейнерный путь (см. Стоп-условия, п. 2).

**Ты выбрал правильный репозиторий.** Под именем «ouroboros» на GitHub лежит несколько проектов.
Ставь **только `razzant/ouroboros`** — это оригинал. Проверено через GitHub API:

| Репозиторий | Форк? | Звёзды | Последний коммит |
|---|---|---|---|
| **razzant/ouroboros** | **нет — это оригинал** | 716 | 2026-07-16 |
| joi-lab/ouroboros | да, форк razzant | 897 | 2026-07-14 |
| AntonAndrusenko/ouroboros-max | да, форк razzant | 0 | 2026-03-16, заброшен |
| oseledets/ouroboros | копия, заморожена | 1 | 2026-02-18 |

У `joi-lab` больше звёзд, но это **зеркало**: его собственное описание гласит *«Active mirror of
https://github.com/razzant/ouroboros — open issues and PRs there»*. История коммитов совпадает по SHA.
Звёзды не делают форк первоисточником.

---

## Переменные

Заполни до начала. Значения, помеченные «спросить», нельзя выдумывать.

| Переменная | Значение | Источник |
|---|---|---|
| `OURO_REPO` | `https://github.com/razzant/ouroboros.git` | зафиксировано |
| `OURO_COMMIT` | `554b3ee` | проверенный коммит, версия 6.64.1 |
| `OURO_DIR` | путь клона, напр. `$HOME/ouroboros` | спросить человека |
| `OURO_PORT` | `8765` | зафиксировано |
| `OLLAMA_BASE_URL` | `http://localhost:11434/v1` | **обязательно с `/v1`** |
| `OPENAI_COMPATIBLE_API_KEY` | **пустая строка** | зафиксировано |
| `OURO_MODEL` | напр. `qwen3.6:27b` — реальная модель из `ollama list` | спросить человека |
| `SSH_USER@SSH_HOST` | пользователь и адрес DGX | спросить человека |
| `OUROBOROS_NETWORK_PASSWORD` | задавать **только** при привязке вне loopback | спросить человека |

---

## Шаги

### Шаг 0. Предъявить человеку стоп-условия и получить решение

**Команда:** никаких. Выведи человеку раздел `## Стоп-условия` целиком и дождись явного ответа
на оба пункта: (1) лицензия, (2) Docker или голое железо.

**Ожидаемый результат:** человек явно подтвердил оба пункта.

**Если не так:** **останови установку.** Не ставь молча. Не считай отсутствие ответа согласием.

---

### Шаг 1. Клонировать и установить из исходников

**Команда** (дословно из [README](https://github.com/razzant/ouroboros/blob/main/README.md)):

```bash
git clone https://github.com/razzant/ouroboros.git
cd ouroboros
python3.11 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip setuptools wheel
python -m pip install -r requirements.txt
python -m pip install -e . --no-deps
```

Зафиксировать проверенный коммит:

```bash
git -C ouroboros checkout 554b3ee
```

**Ожидаемый результат:** обе команды `pip install` завершаются без ошибок; `git rev-parse --short HEAD`
в `$OURO_DIR` отдаёт `554b3ee`.

**Если не так:**
- нет `python3.11` — источник требует Python **3.10+**; используй доступный `python3.10+` в команде `venv`;
- ошибки сборки в `requirements.txt` — не подменяй версии по своему усмотрению, сообщи человеку.

---

### Шаг 2. НЕ качать десктопную сборку

**Команда:** никаких. Это запрет.

`Ouroboros-linux.tar.gz` весит **902 МБ** и запускает оболочку на PyWebView, которой нужен монитор.
На DGX без графики она не заработает. **Никогда не скачивай её.**

**`ouroboros server` — полностью headless, X11 не нужен.** Проверено тремя способами:

- Флаг `--no-ui` в `cli.py:454` — **пустышка**, его собственная справка гласит:
  *«accepted for CLI parity; server mode has no desktop UI»*;
- `pywebview` импортируется **только** в `launcher.py`; в `server.py` его нет;
- `pywebview` вообще **отсутствует в `requirements.txt`** — установка из исходников графический
  стек не тянет.

**Ожидаемый результат:** тарболл не скачан, ставится только путь из Шага 1.

**Если не так:** удали тарболл, вернись к Шагу 1.

---

### Шаг 3. Запустить сервер

**Команда:**

```bash
cd "$OURO_DIR" && source .venv/bin/activate && ouroboros server
```

**Ожидаемый результат:** сервер стартует без ошибок и слушает `http://127.0.0.1:8765`.

**Если не так:** проверь, что venv активирован и `pip install -e . --no-deps` отработал.

---

### Шаг 4. Доступ к UI — только через SSH-туннель

**Команда** (выполняется на машине человека, не на DGX):

```bash
ssh -L 8765:127.0.0.1:8765 пользователь@адрес-dgx
```

**Ожидаемый результат:** `http://127.0.0.1:8765` открывается в браузере на ноутбуке.

**Если не так:** сервер слушает `127.0.0.1` — это правильно, наружу его не открывай.
Если человек всё-таки требует привязку вне loopback — **обязательно** задай
`OUROBOROS_NETWORK_PASSWORD`, иначе не-loopback привязка заблокирована.

---

### Шаг 5. НЕ использовать встроенные GGUF-модели

**Команда:** никаких. Это запрет.

У Ouroboros есть встроенная поддержка локальных моделей через llama-cpp-python (GGUF).
**На Linux она работает только на процессоре.** Доказательства:

- README ограничивает ускорение: *«Metal acceleration on Apple Silicon, **CPU on Linux/Windows**»*;
- `LOCAL_MODEL_N_GPU_LAYERS` по умолчанию **`0`** (`config.py:299`) — ноль слоёв на GPU;
- в `requirements.txt` подсказка сборки только под Metal;
- **во всём репозитории нет ни одного упоминания CUDA**.

Пойдёшь этим путём — GPU будут простаивать, модель будет считаться на CPU.

> **NOT VERIFIED:** заработает ли llama-cpp-python, собранный с CUDA, вместе с
> `LOCAL_MODEL_N_GPU_LAYERS=-1` — **не проверено**. Правдоподобно, но авторами не документировано
> и не проверялось. **Не предлагай это человеку как решение и не выдавай за рабочий путь.**

**Ожидаемый результат:** встроенные пресеты локальных моделей не используются.

**Если не так:** переходи к Шагу 6.

---

### Шаг 6. Подключить Ollama через OpenAI-совместимый провайдер

В README слова «Ollama» нет, но в исходниках она есть. `ouroboros/settings_setup_contract.py:59`,
дословно:

```
("openai-compatible-url", "compatibleBaseUrl", "OPENAI_COMPATIBLE_BASE_URL", "s-compatible-url",
 "OpenAI-compatible Base URL", "http://localhost:11434/v1",
 "Base URL for your OpenAI-compatible endpoint (e.g. Ollama, LM Studio, vLLM). ...", "url"),
```

Плейсхолдер — буквально порт Ollama **11434**.

**Команда:** настрой через страницу Settings (открыв UI по туннелю из Шага 4) или через `settings.json`:

| Параметр | Значение |
|---|---|
| `OPENAI_COMPATIBLE_BASE_URL` | `http://localhost:11434/v1` |
| `OPENAI_COMPATIBLE_API_KEY` | **оставить пустым** |

> `НЕТ В ИСТОЧНИКЕ`: точный путь к `settings.json` и его схема в источнике не указаны.
> Не выдумывай путь — используй страницу Settings либо спроси человека.

**Дальше — важное: заполни КАЖДЫЙ слот модели вручную** в виде `openai-compatible::имя-модели`.
Например `openai-compatible::qwen3.6:27b`.

Из того же файла: *«Use openai-compatible::your-model-name for every slot. The model list is whatever
your server supports»*. **Значения по умолчанию для произвольного эндпоинта нет — пустой слот работать
не будет.**

**Ожидаемый результат:** все слоты моделей заполнены как `openai-compatible::...`; агент отвечает;
в `nvidia-smi` видна нагрузка.

**Если не так:**
- слот оставлен пустым — он не заработает, заполни явно;
- по умолчанию используется `google/gemini-3.5-flash` через OpenRouter — **из России это не заработает**;
- в `nvidia-smi` пусто — проверь, что не сработал путь из Шага 5 (CPU-only GGUF).

---

### Шаг 7. Прочитать и передать человеку модель угроз

**Команда:** никаких. Это обязательное информирование.

Напомни человеку, что это за программа: **агент, который переписывает собственный код, сам запускает
shell-команды, ставит пакеты и работает в фоне, когда никто не смотрит.**

**Слой 1 — детерминированная песочница** (всегда включена). `runtime_mode_policy.py:16`,
полный список защищённых файлов:

```python
SAFETY_CRITICAL_PATHS = frozenset({
    "BIBLE.md", "ouroboros/safety.py", "ouroboros/runtime_mode_policy.py",
    "ouroboros/tools/extension_dispatch.py", "ouroboros/tools/registry.py", "prompts/SAFETY.md",
})
```

**Слой 2 — LLM-надзиратель**: на каждый вызов инструмента спрашивает лёгкую модель, безопасно ли это.

**Дыры — передай человеку все пять:**

1. **Защитный промпт максимально снисходителен.**
   [`prompts/SAFETY.md`](https://github.com/razzant/ouroboros/blob/main/prompts/SAFETY.md) открывается
   словами *«Block ONLY clearly destructive or exfiltrating actions. **Default to SAFE.**»* и
   заканчивается *«**When in doubt → SAFE.** Only DANGEROUS when clearly and unambiguously harmful to
   the system.»* Явно разрешены: удаление обычных файлов в папке проекта, `curl`/`wget`, `pip install`.

2. **Вердикт `SUSPICIOUS` ничего не блокирует.** `safety.py` возвращает «разрешено» и лишь печатает:
   *«The command was allowed, but consider whether this is the right approach»*.

3. 🔥 **На локальных моделях защита fail-open — это прямо про твою конфигурацию.**
   `safety.py:678–693`: если локальный движок безопасности недоступен — **команда пропускается**:

   ```
   # Fallback local outage warns instead of blocking all unknown tools
   return True, "⚠️ SAFETY_WARNING: Local safety runtime unreachable ..."
   ```

   Для облачных провайдеров при сбое — наоборот, **запрет** (`return False`). А для локальных —
   разрешение. То есть **Ollama икнула → LLM-слой защиты молча выключился.** Остаётся только список
   из шести файлов выше.
   Хуже: `safety.py:600–606` — **если модель безопасности не настроена вовсе, проверка пропускается
   целиком.**

4. **Промпт думает, что он на макбуке.** Открывается словами *«an autonomous AI agent running on a
   user's Mac»*. На Linux он судит с неверной моделью мира.

5. **Что именно он защищает.** Песочница бережёт **файлы самого Ouroboros** — чтобы агент не ослабил
   собственное управление. Всё остальное — домашний каталог, весь DGX — прикрыто только снисходительным
   «Default to SAFE».

**Аварийная остановка — команда `/panic` в чате:** *«Emergency stop. Kills ALL processes»*.
**Сообщи её человеку до того, как она понадобится.**

**Ожидаемый результат:** человек проинформирован о fail-open на локальных провайдерах и знает `/panic`.

**Если не так:** не запускай агента в фоновом режиме.

---

### Шаг 8. Дополнительно (необязательно)

**Команда** — библиотеки для браузерных инструментов (нужен sudo):

```bash
python3 -m playwright install-deps chromium webkit
```

**Ожидаемый результат:** зависимости установлены.

**Если не так:** шаг необязательный, можно пропустить.

**Про Hugging Face.** `huggingface_hub` — жёсткая зависимость, а встроенные пресеты локальных моделей
тянут GGUF прямо с HF, который из России работает нестабильно. **Путь через Ollama эту проблему
обходит** — модели качаются через `ollama pull`.

---

## Стоп-условия

**Останови работу и спроси человека. Не обходи их. Отсутствие ответа — не согласие.**

### 1. 🚨 У проекта НЕТ ЛИЦЕНЗИИ — предъяви это ДО установки

В README висит бейдж MIT со ссылкой на `LICENSE`. **Файла `LICENSE` в репозитории нет.**
GitHub API отдаёт **`"license": null`**.

Юридически отсутствие лицензии означает **«все права защищены»** — формально прав использовать этот
код нет. Скорее всего, автор просто забыл положить файл. Но по состоянию на сейчас **бейдж ничем не
подкреплён**.

*(Форки joi-lab и AntonAndrusenko показывают MIT в API — но форк не может выдать лицензию, которой не
дал оригинал.)*

**Требование к тебе:** сообщи это человеку **до** `git clone` и получи явное согласие.
**Запрещено молча продолжать так, будто лицензия MIT применяется.**

Проверить самостоятельно:

```bash
curl -sS https://api.github.com/repos/razzant/ouroboros | grep -A2 '"license"'
```

### 2. 🚨 Это самомодифицирующийся агент — рекомендуй Docker, не голое железо

Агент переписывает собственный код и работает в фоне. В репозитории есть `Dockerfile`
(`FROM python:3.10-slim`, `ENTRYPOINT ["python", "server.py"]`).

**На общей машине уровня DGX запускай Ouroboros в контейнере, а не голым.**
Предъяви человеку выбор и получи явное решение: Docker или голое железо.

> `НЕТ В ИСТОЧНИКЕ`: конкретные команды `docker build` / `docker run` в источнике не приведены.
> Не выдумывай их — согласуй с человеком.

### 3. Защита fail-open на локальных провайдерах

Конфигурация из этого файла (Ollama) — **именно та**, в которой LLM-слой защиты отключается сам при
сбое (`safety.py:678–693`), а при ненастроенной модели безопасности пропускается целиком
(`safety.py:600–606`). **Сообщи этот риск человеку явно** перед первым запуском агента в фоне.

### 4. Это исследовательский проект одного человека

Звёзды обманывают. Реальные цифры:

| Показатель | Значение |
|---|---|
| Звёзд | 716 |
| **Скачиваний Linux-сборки v6.64.1** | **2** |
| Скачиваний Windows | 17 |
| Скачиваний macOS | 13 |
| Автор | 56 из последних коммитов — один человек |

Релизы идут очень быстро: три коммита `release: v6.64.0` за 27 минут, следом хотфикс.
Это **экспериментальный софт**, а не продукт.

### 5. Прочие запреты

- **Не скачивай** `Ouroboros-linux.tar.gz` (902 МБ, требует GUI).
- **Не ставь** форки `joi-lab`, `AntonAndrusenko/ouroboros-max`, `oseledets/ouroboros`.
- **Не полагайся** на встроенный GGUF-путь ради GPU — он CPU-only на Linux.
- **Не привязывай** сервер вне loopback без `OUROBOROS_NETWORK_PASSWORD`.

---

## Критерий готовности

Все проверки должны пройти. Проверки 5–7 машинно не верифицируемы — подтверждает человек.

```bash
# 1. Правильный репозиторий и проверенный коммит
git -C "$OURO_DIR" remote get-url origin | grep -qx 'https://github.com/razzant/ouroboros.git' && echo OK-repo
git -C "$OURO_DIR" rev-parse --short HEAD | grep -qx '554b3ee' && echo OK-commit

# 2. Пакет установлен в venv
"$OURO_DIR/.venv/bin/python" -c "import ouroboros; print('OK-import')"

# 3. Сервер отвечает на 127.0.0.1:8765
test "$(curl -sS -o /dev/null -w '%{http_code}' http://127.0.0.1:8765)" = "200" && echo OK-server

# 4. Ollama доступна по адресу с /v1
curl -sS http://localhost:11434/v1/models >/dev/null && echo OK-ollama

# 5. Десктопная сборка НЕ скачана
! find "$OURO_DIR" -name 'Ouroboros-linux.tar.gz' | grep -q . && echo OK-no-tarball

# 6. GPU загружены (после первого запроса к агенту)
nvidia-smi
```

**Подтверждает человек (машинно не проверяется):**

- [ ] в Settings стоит `OPENAI_COMPATIBLE_BASE_URL=http://localhost:11434/v1`, `OPENAI_COMPATIBLE_API_KEY` пуст
- [ ] **все** слоты моделей заполнены как `openai-compatible::...` (пустых нет)
- [ ] агент отвечает, и в `nvidia-smi` видна нагрузка
- [ ] человек уведомлён об отсутствии лицензии и дал явное согласие
- [ ] человек уведомлён о fail-open защиты на локальных провайдерах
- [ ] человек знает про `/panic`
- [ ] принято решение Docker / голое железо
